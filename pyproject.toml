[project]
name = "users-microservice"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.135.1",
    "sqlalchemy"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "requests",
]
